week02
- linux commands
  ls, ls -a, ls -l, ls -al
  pwd, cd .git, cd .., touch week01.txt
  vi week01.txt, vi week02.txt

- git commands
  git init
  git status
  git add week01.txt
  git config --global user.email "blahblah@examle.com"
  git config --global user.name "blahblah"
  git commit -m "v0"

